<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GRIMLINGS - <%- pageTitle %></title>
    <link rel="stylesheet" href="/public/css/style.css">
    <link rel="stylesheet" href="/public/css/header.css">
</head>

<body>
    <div class="header">

        <h1 id="logo" onclick="window.location.href = '/'">GRIMLINGS</h1>
        <% if(isLoggedIn) { %>
            <h2 id="foodCount">Times fed: <span id="foodCounter">0</span></h2>
            <!--
            Don't need these for the MVP
        <a href='/explore'><button class="headerButton">EXPLORE</button></a>
    -->
            <a href='/pet/createpet'><button class="headerButton">Create New Pet</button></a>
            <!--
            Don't need these for the MVP
        <a href='/shop'><button class="headerButton">SHOP</button></a>
        <a href='/inventory'><button class="headerButton">INVENTORY</button></a>
    -->
            <% } %>
    </div>